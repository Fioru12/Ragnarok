# Dipendenze del RAG Engine (ChromaDB + embedding ONNX locali).
# Installazione opzionale ma raccomandata:
#   pip install -r requirements.txt -r requirements-rag.txt
chromadb>=0.5.0
fastembed>=0.3.0
fpdf2>=2.7
